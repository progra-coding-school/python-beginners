# When you divide any whole number by 100,
# the remainder (% 100) is always the last 2 digit.
# Divide by 100 → the leftover is the last 2 digit
#Ex:86 % 10  → 86   # ones (last digit)

last_two_digit=86%100
print(last_two_digit)

last_two_digit=1986%100
print(last_two_digit)